# Full & Distlist backup compression and Google Drive upload also remove old backup file from google drive
# Runs every day at 03:30 AM
# Upload the file to /usr/local/bin/
# Edit crontab -e
# List crontab -l

30 3 * * * /usr/local/bin/upload.sh >> /var/log/backup_upload.log 2>&1
